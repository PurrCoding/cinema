-- Some changes to the cinema_construct map, because the author does not update the map anymore.

-- Remove the hooks for adding prop_dynamic doors
hook.Remove("InitPostEntity", "CinemaConstructInitGamemode")